import sum, * as math from "./library.js";
// import sum, { times, subtract } from "./library.js";

const { times, subtract } = math;

console.log(sum(10, 20));
console.log(times(10, 20));
console.log(subtract(10, 20));